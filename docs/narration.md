# Построение повествования

В Ren'Py повествованием назвается процесс описания и представления игроку происходящих в сюжете произведения событий. Всё повествование происходит в отдельных Ren'Py блоках, называемых [лэйблами](#лэйблы-35), и в основном состоит из вывода реплик, манипуляций над изображениями, проигрыванием аудио и прочих действий.

```renpy
label day3_main2: # game\scenario\day3.rpy:311
    window show
    "Я дожёвывал сосиску, которая больше напоминала кусок витой пары."
    me "Интересно, есть у них здесь интернет?"
    me "Откуда?"
    "Пионер за соседним столом заметил, что я разговариваю сам с собой, покосился на меня, а я в ответ улыбнулся и помахал ему рукой."
    "После такого дружелюбного приветствия он почему-то быстрыми шагами покинул столовую, даже не доев завтрак."
    stop ambience fadeout 2
    th "С едой покончено – самое время отправиться на поиски разгадок."
    window hide
```

## Лэйблы

**Лэйблы** (или просто **метки**, от англ. *Label*) — это специальная метка, которая позволяет повествованию перемещаться в конкретные места в сюжете.

Cинтаксис лэйбла выглядит так:

```renpy
label |Название лэйбла|:
    |Дальнейшие команды| # Всё дальнейшие команды могут быть написаны без отступа в начале, но мы не рекомендуем этого делать.
    return # Эта команда возвращает повествование к предыдущему лэйблу после вызова или возвращает игрока в главное меню.
```

:::warning
Во избежание конфликтов имя каждого лэйбла должно быть уникальным и отличаться от всех остальных.
В больших модах название лейбла часто состоит из сокращённого названия мода, ключевого слова `label` или `lbl` и названия сцены или номера дня/главы/эпизода/прочего.
Пример — `mymod_label_day1`.
:::

Для перемещения между лэйблами существует две команды: `jump` и `call`.

Команда `jump` (от англ. *прыжок*) просто перемещает повествование в новый лэйбл.
Пример:

```renpy
label start:
    "Начало"
    jump new_label
    "После прыжка" # До сюда повествование не дойдёт, так как мы использовали команду jump

label new_label:
    "Новый лэйбл"
```

Команда `call` (от англ. *вызов*) перемещает повествование в новый лэйбл и позволяет вернуться в предыдущий лэйбл когда повествование доходит до команды `return`.
Пример:

```renpy
label start:
    "Начало"
    call new_label
    "После вызова" # Вместо jump была использована команда call и этот текст будет выведен читателю

label new_label:
    "Новый лэйбл"
    return
```

## Вывод реплик

Чтобы вывести какую-либо реплику какого-либо персонажа в повествовании используется внутренняя команда, которая состоит из двух частей: Объект персонажа/имя персонажа строкой и сама реплика.

Примеры:

```renpy
    # ...
    "Привет!" # На экран выведется текст "Привет!" без имени персонажа
    "Персонаж" "Привет!" # На экран выведется текст "Привет!" от персонажа "Персонаж"
    sl "Привет!" # На экран выведется текст "Привет!" от Слави
    # ...
```

:::info
**Имейте ввиду!**

Чаще всего для удобства для каждого персонажа создаётся переменная, в которой и хранится его объект. Подробнее об этом читайте [ниже](#).

Если в команду передана только одна строка текста, то она будет восприниматься как реплика от внутреннего персонажа `narrator` — рассказчика, у которого нет видного игроку имени.

Узнать какие переменные отвечают за каких персонажей можно на странице с [ресурсами оригинальной игры](/resources/original/characters).
:::

## Особые функции повествования

В игре "Бесконечное Лето" имеется несколько особых функций, которые вызываются во время повествования.

### Изменение стиля интерфейса

При использовании стандартного интерфейса новеллы во время повествования можно изменить его стиль. Для этого в повествовании нужно вызвать фунцкию смены интерфейса.

Пример:

```renpy
    # ...
    $ day_time() # Дневной стиль интерфейса
    "Днём было светло."
    $ sunset_time() # Утренний/вечерний стиль
    "Но день подходил к концу."
    $ night_time() # Ночной стиль
    "А ночью было темно."
    $ prolog_time() # Стиль пролога
    "И холодно."
    # ...
```

Функции и соответствующий им интерфейс:

1. Пролог (`$ prolog_time()`)
> ![Диалоговое окно](/resource/raw/images/gui/dialogue_box/prologue/dialogue_box.png "Диалоговое окно")
![Стрелка назад](/resource/raw/images/gui/dialogue_box/prologue/backward_idle.png "Стрелка назад")
![Стрелка вперёд](/resource/raw/images/gui/dialogue_box/prologue/forward_idle.png "Стрелка вперёд")

2. День (`$ day_time()`)
> ![Диалоговое окно](/resource/raw/images/gui/dialogue_box/day/dialogue_box.png "Диалоговое окно")
![Стрелка назад](/resource/raw/images/gui/dialogue_box/day/backward_idle.png "Стрелка назад")
![Стрелка вперёд](/resource/raw/images/gui/dialogue_box/day/forward_idle.png "Стрелка вперёд")

3. Закат (`$ sunset_time()`)
> ![Диалоговое окно](/resource/raw/images/gui/dialogue_box/sunset/dialogue_box.png "Диалоговое окно")
![Стрелка назад](/resource/raw/images/gui/dialogue_box/sunset/backward_idle.png "Стрелка назад")
![Стрелка вперёд](/resource/raw/images/gui/dialogue_box/sunset/forward_idle.png "Стрелка вперёд")

4. Ночь (`$ night_time()`)
> ![Диалоговое окно](/resource/raw/images/gui/dialogue_box/night/dialogue_box.png "Диалоговое окно")
![Стрелка назад](/resource/raw/images/gui/dialogue_box/night/backward_idle.png "Стрелка назад")
![Стрелка вперёд](/resource/raw/images/gui/dialogue_box/night/forward_idle.png "Стрелка вперёд")


### Изменение оттенка спрайтов

При использовании стандартных спрайтов новеллы во время повествования можно изменить их оттенок. Для этого в повествовании нужно изменить постоянную переменную "времени спрайтов".

Пример:

```renpy
    # ...
    show sl normal pioneer
    $ persistent.sprite_time = "day" # Дневной оттенок (без оттенка)
    pause
    $ persistent.sprite_time = "sunset" # Утренний/вечерний оттенок
    pause
    $ persistent.sprite_time = "night" # Ночной оттенок
    pause
    # ...
```

:::warning
**Внимание!**

Смена оттенка не распространяется на некоторые спрайты оригинальной новеллы и на все пользовательские спрайты! Их нужно будет красить самому или добавлять проверки как в оригинальной новелле! Пример покраски спрайта с проверкой можно найти в [файлах скриптов игры](/resource/raw/sprites.rpy).
:::

:::info
**Имейте ввиду!**

Как такового "прологового" оттенка в игре нет, в эпилоге (где используется прологовый стиль интерфейса) использован ночной оттенок.
:::

## ADV и NVL режимы повествования

 Существует два основных стиля изложения материала, используемых в визуальных новеллах:

1. `ADV`: Визуальные новеллы, созданные в стиле ADV, представляют собой диалог и повествование, выводимые на экран по одной строке за раз, как правило, в окне, расположенном в нижней части экрана.
2. `NVL`: Визуальные новеллы, созданные в стиле NVL, представляют собой несколько строк на экране, выводимые одновременно в окне, которое занимает весь экран.

По умолчанию меню отображаются в ADV-режиме, занимая весь экран. Существует также альтернативное представление меню в NVL-режиме, в котором меню отображаются сразу после текущей страницы текста NVL-режима.

Для перехода в NVL-режим повествования используйте следующую команду: `$ set_mode_nvl()`

Для перехода обратно в ADV-режим повествования используйте следующее: `$ set_mode_adv()`

Примеры перехода в NVL и ADV режимы повествования:

```renpy
label my_mod:
    "По умолчанию стоит adv-режим повествования."
    $ set_mode_nvl()
    "Теперь повествование ведётся в nvl-режиме."
    $ set_mode_adv()
    "Повествование вернулось обратно в adv-режим."
```


:::info
**Плавные переходы окон**

Не забывайте про плавные переходы для окон, в которых ведётся повествование.

Для ADV-режима:

- `window hide` — плавное скрытие диалогового окна.
- `window show` — плавное появление диалогового окна.

Для NVL-режима:

- `nvl show dissolve` — плавное появление NVL-окна.
- `nvl hide dissolve` — плавное скрытие NVL-окна.

Пример использования плавных переходов окон выглядит так:

```renpy
label my_mod:
    "Повествование в adv-режиме."

    window hide # Плавное скрытие окна
    pause(1) # Секундная пауза, чтобы сокрытие окна успело произойти до смены режима повествования
    $ set_mode_nvl() # Переключение на новелльный режим повествования
    nvl show dissolve # Плавное появление nvl-окна

    "Повествование в nvl-режиме."

    nvl hide dissolve # Плавное скрытие nvl-окна
    pause(1) # Секундная пауза, чтобы сокрытие окна успело произойти до смены режима повествования
    $ set_mode_adv() # Переключение на adv-режим повествования
    window show # Плавное появление диалогового окна

    "Повествование в adv-режиме."
```

:::


:::info
**Появление персонажа в NVL-режиме**

Персонажи могут быть объявлены для использования в NVL-режиме путем создания ещё одного персонажа, но с добавлением параметра kind=nvl. Например:

```renpy
define maks_nvl = Character('Максим', kind=nvl, color="#c8ffc8")
```

Если этого не сделать, то внутри nvl-режима повествования персонажи без параметра `kind=nvl` будут говорить в adv-режиме.

Обратите своё внимание, что можно сделать и по другому — сделать цикл, который будет менять объявление персонажей в зависимости от текущего режима повествования. Именно такой способ и используется в БЛ.


:::

##  Оформление текста

 Примеры текстовых тегов:

1. **alpha**

Текстовый тег `alpha` отображает текст, расположенный между самим тегом и его закрывающим тегом, с указанной непрозрачностью. Непрозрачность должна иметь значение между 0.0 и 1.0, что соответствует полной прозрачности и полной непрозрачности соответственно. Если значение начинается с префикса `+` (добавить на) или `-` (убавить на), непрозрачность будет изменена на указанное число, а не полностью заменена. Если значение начинается с префикса `*` (умножить на), непрозрачность будет умножена на это число.

```renpy
"{alpha=0.1}Этот текст едва читаем!{/alpha}"
"{alpha=-0.1}Этот текст на 10 процентов прозрачнее, чем текст по умолчанию.{/alpha}"
"{alpha=*0.5}Этот текст наполовину такой же непрозрачный, как и по умолчанию.{/alpha}"
```

2. **b** (bold)

Тег жирного шрифта выводит текст, расположенный между самим тегом и его закрывающим тегом, жирным шрифтом.

```renpy
"Пример {b}текста, выделенного жирным шрифтом{/b}."
```

3. **color**

Текстовый тег `color` отображает текст, расположенный между самим тегом и его закрывающим тегом, в указанном цвете. Цвет должен быть в формате #rgb, #rgba, #rrggbb или #rrggbbaa.

```renpy
"{color=#f00}Красный{/color}, {color=#00ff00}Зелёный{/color}, {color=#0000ffff}Синий{/color}"
```

4. **i** (italic)

Тег курсива отображает текст, расположенный между самим тегом и его закрывающим тегом, курсивом.

```renpy
mt "Добро пожаловать в {i}СОЛ «Совёнок»!{/i}."
```

5. **s** (strikethrough)

Тег зачеркивания проводит черту через текст, расположенный между тегом и его закрывающим тегом.

```renpy
mi "Я очень надеюсь, что {s}тебе понравилось{/s}."
```

6. **u** (underline)

Тег подчеркивания подчеркивает текст, расположенный между самим тегом и его закрывающим тегом.

```renpy
mi "Рада видеть {u}тебя{/u}!"
```

7. **w** (wait)

Тег ожидание – самозакрывающийся тег, который ждёт, пока читатель нажмёт на кнопку для продолжения. Если для тега задан аргумент в форме числа, то ожидание автоматически заканчивается, когда заданные секунды проходят.

```renpy
un "Я{w} очень{w=1.0} рада!"
```

Полный список текстовых тегов можно найти внутри [соответствующей статьи в документации Ren'Py](http://ru.renpypedia.shoutwiki.com/wiki/%D0%A2%D0%B5%D0%BA%D1%81%D1%82_(Text)).

## Опасная зона!

Получить больше информации о повествовании вы можете в [официальной документации Ren'Py](https://www.renpy.org/doc/html/dialogue.html).