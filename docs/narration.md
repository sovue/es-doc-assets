# Построение повествования

В Ren'Py повествованием назвается процесс описания и представления игроку происходящих в сюжете произведения событий. Всё повествование происходит в отдельных Ren'Py блоках, называемых [лэйблами](#лэйблы), и в основном состоит из [вывода реплик](#вывод-реплик), манипуляций над изображениями, проигрыванием аудио и прочих действий.

```renpy
label day3_main2: # game\scenario\day3.rpy:311
    window show
    "Я дожёвывал сосиску, которая больше напоминала кусок витой пары."
    me "Интересно, есть у них здесь интернет?"
    me "Откуда?"
    "Пионер за соседним столом заметил, что я разговариваю сам с собой, покосился на меня, а я в ответ улыбнулся и помахал ему рукой."
    "После такого дружелюбного приветствия он почему-то быстрыми шагами покинул столовую, даже не доев завтрак."
    stop ambience fadeout 2
    th "С едой покончено – самое время отправиться на поиски разгадок."
    window hide
```

## Лэйблы

**Лэйблы** (или просто **метки**, от англ. *Label*) — это специальная метка, которая позволяет повествованию перемещаться в конкретные места в сюжете.  
Cинтаксис:

```renpy
label |Название лэйбла|:
    |Дальнейшие команды| # Всё дальнейшие команды могут быть написаны без отступа в начале, но мы не рекомендуем этого делать.
```

:::warning
Во избежание конфликтов имя каждого лэйбла должно быть уникальным и отличаться от всех остальных.
В больших модах название лейбла часто состоит из названия сцены или номера дня/главы/эпизода/прочего и постфикса или префикса короткого названия мода.
Пример:
```
mymod_day2
↑     ↑
│     Название сцены
Префикс (в виде названия мода)

Или...

day2_mymod
↑    ↑
│    Постфикс (в виде названия мода)
Название сцены
```
:::

### Глобальные и локальные лэйблы

Ранее показанные лэйблы являются глобальными, и вызвать их можно из любой части игры.
Но помимо глобальных существуют и локальные лэйблы.
Они всегда принадлежат своему глобальному лэйблу и не могут существовать без него.  
Cинтаксис:

```renpy
label |Название глобального лэйбла|:
    |Команды|
label .|Название локального лэйбла|: # Краткое написание, принадлежит первому найденному глобальному лэйблу
    |Команды|
label |Название глобального лэйбла|.|Название локального лэйбла|: # Полное написание, принадлежит к глобальному лэйблу перед точкой
    |Команды|
```

Если повествование находится в глобальном лэйбле с подконтрольными ему локальными лэйблами, то на них можно перемещаться, используя только названия локального лэйбла.  
Пример:

```renpy
label global_label:
    "Внутри этого глобального лэйбла есть..."
    jump .local_label

label .local_label:
    "...локальный лэйбл!"
```

Также можно переместиться в локальный лэйбл другого глобального лэйбла.
Для этого нужно использовать связку в виде названия глобального лэйбла и названия локального лэйбла, разделённых точкой.  
Пример:

```renpy
label global_label:
    "Мы в глобальном лэйбле."
    jump another_global.local_label

label another_global:
    "Учите английский язык!"

label .local_label:
    "А теперь мы в локальном лэйбле другого глобального лэйбла."
```

### Перемещение между лэйблами

:::info
Далее в статье будет использован такой термин, как **стэк**.

Стэк можно описать как список точек, с которых продолжится повествование после того, как оно дойдёт до команды `return`.
Каждая команда `call` добавляет такую точку после себя, а команда `return` возвращает повествование на последнюю такую точку и удаляет её из стэка.

Если точек в этом списке не осталось и повествование доходит до команды `return`, то игра перезапускается, что создаёт эффект возвращения в главное меню.
:::

Для перемещения между лэйблами существует три команды: `jump`, `call` и `return`.

1. Команда `jump` (от англ. *прыжок*) перемещает повествование в новый лэйбл не обновляя стэк.  
Пример:

```renpy
label start:
    "Начало"
    jump next_label # Перемещаемся в next_label...
    "После прыжка" # До сюда повествование не дойдёт, так как этой точки нет в стэке

label next_label:
    "Следующий лэйбл"
```

2. Команда `call` (от англ. *вызов*) перемещает повествование в новый лэйбл обновляя стэк, что позволяет вернуться в предыдущий лэйбл командой `return`.  
Пример:

```renpy
label start:
    "Начало"
    call next_label # Перемещаемся в next_label...
    "После вызова" # Вместо jump была использована команда call и этот текст будет выведен читателю

label next_label:
    "Следующий лэйбл"
    return # Возвращаем повествование в лэйбл "start"...
```

3. Команда `return` (от англ. *возврат*) удаляет последнюю команду из стэка и возвращает повествование к ней. Если стэк пуст — возвращает игрока в главное меню.  
Пример:

```renpy
label start:
    "Начало"
    call next_label # Перемещаемся в next_label...
    "После вызова"
    return # Стэк пуст! Возвращаем игрока в главное меню...

label next_label:
    "Следующий лэйбл"
    return # Возвращаемся к последнему лэйблу в стэке...
```

:::info
Если перед концом лэйбла не стоит команда `return`, то повествование пойдёт дальше и достигнет следующего лэйбла.
Если же после конца лэйбла нет других лэйблов, то игра закончится, и игрок вернётся в главное меню.  
Пример:

```renpy
label start:
    "Начало"
    # Здесь ничего не осталось, дальше только следующий лэйбл

label next_label:
    "Следующий лэйбл" # А после этой команды ничего нет, и игрока вернёт в главное меню
```
:::

## Вывод реплик

Чтобы вывести какую-либо реплику какого-либо персонажа в повествовании используется внутренняя команда, которая состоит из двух частей: Вызываемого объекта персонажа/имени персонажа строкой и реплики.

```renpy
    "|Реплика|"
    "|Имя персонажа|" "|Реплика|"
    |Вызываемый объект| "|Реплика|"
```

Примеры:

```renpy
    # ...
    "Привет!" # На экран выведется текст "Привет!" без имени персонажа
    "Персонаж" "Привет!" # На экран выведется текст "Привет!" от персонажа "Персонаж"
    sl "Привет!" # На экран выведется текст "Привет!" от Слави
    # ...
```

:::info
Если в команду передана только одна строка текста, то она будет восприниматься как реплика от внутреннего персонажа `narrator` — рассказчика, у которого нет видимого имени.

Чаще всего для удобства для каждого персонажа создаётся переменная, в которой и хранится его объект.
О том, как создавать новых персонажей, читайте в [этой статье](/docs/characters).

Узнать какие переменные отвечают за каких персонажей можно на [странице с ресурсами оригинальной игры](/resources/original/characters).
:::

### Продолжение реплики

Чтобы продолжить предыдущую реплику используется персонаж `extend`.
Предыдущая реплика останется на экране, а следующая начнёт выводиться сразу же после неё.  
Пример:

```renpy
    # ...
    "Я и не думал, что такое может быть."
    extend " Но это может быть."
    #       ↑ Заметьте! Пробел!
    # ...
```

:::attention
Имейте ввиду, что между репликами должен стоять пробел или перенос на следующую строку (`\n`), без них итоговая реплика в итоге не будет иметь пробела!
Пробел должен стоять в конце предыдущей реплики или в начале `extend`'а.

```renpy
    # ...
    "Я и не думал, что такое может быть."
    extend "Но это может быть."
    # ...
```

Получится:

```
Я и не думал, что такое может быть.Но это может быть.
```
:::

## Отображение и скрытие диалоговых окон

Диалоговые окна (также как и окна [NVL-режима](#особые-функции-повествования/изменение-режима-повествования)) можно отображать и скрывать перед выводом реплики. Это также можно делать и плавно, используя [переходы](/docs/transitions)

Чтобы отображать и скрывать диалоговое окно используются команды `window show` и `window hide` соответственно.
К отображению и скрытию диалоговых окон можно применить переходы, если сразу после команды указать объект перехода.  
Пример:

```renpy
    # ...
    "Я выглянул в окно."
    window hide dissolve # Заметьте, что здесь не требуется ключевое слово with
    # ...
    window show dissolve
    "Я сел обратно за стол."
    # ...
```

Чтобы отображать и скрывать окно NVL-режима используются команды `nvl show` и `nvl hide` соответственно.
Также как и с диалоговыми окнами к отображению и скрытию окон NVL-режима можно применить переходы, если сразу после команды указать объект перехода.

Помимо отображения и скрытия окна NVL-режима командой `nvl clear` его можно очистить.  
Пример:

```renpy
    # ...
    "Бла-бла-бла."
    "Бла-бла-бла-бла."
    "Бла-бла."
    nvl clear # Все предыдущие реплики пропадут с экрана NVL-режима
    "Бла."
    # ...
```

## Особые функции повествования

В игре "Бесконечное Лето" в повествовании также можно изменять ещё некоторые параметры игры:
изменять стиль интерфейса, оттенок спрайтов или включать NVL-режим для ванильных^1 персонажей.

### Изменение стиля интерфейса

При использовании стандартного интерфейса новеллы во время повествования можно изменить его стиль. Для этого в повествовании нужно вызвать фунцкию смены интерфейса.

Пример:

```renpy
    # ...
    $ day_time() # Дневной стиль интерфейса
    "Днём было светло."
    $ sunset_time() # Утренний/вечерний стиль
    "Но день подходил к концу."
    $ night_time() # Ночной стиль
    "А ночью было темно."
    $ prolog_time() # Стиль пролога
    "И холодно."
    # ...
```

Функции и соответствующий им интерфейс:

1. Пролог (`$ prolog_time()`)
> [![Диалоговое окно](/resource/raw/images/gui/dialogue_box/prologue/dialogue_box.png "Диалоговое окно")](/resource/raw/images/gui/dialogue_box/prologue/dialogue_box.png)
[![Стрелка назад](/resource/raw/images/gui/dialogue_box/prologue/backward_idle.png "Стрелка назад")](/resource/raw/images/gui/dialogue_box/prologue/backward_idle.png)
[![Стрелка вперёд](/resource/raw/images/gui/dialogue_box/prologue/forward_idle.png "Стрелка вперёд")](/resource/raw/images/gui/dialogue_box/prologue/forward_idle.png)

2. День (`$ day_time()`)
> [![Диалоговое окно](/resource/raw/images/gui/dialogue_box/day/dialogue_box.png "Диалоговое окно")](/resource/raw/images/gui/dialogue_box/day/dialogue_box.png)
[![Стрелка назад](/resource/raw/images/gui/dialogue_box/day/backward_idle.png "Стрелка назад")](/resource/raw/images/gui/dialogue_box/day/backward_idle.png)
[![Стрелка вперёд](/resource/raw/images/gui/dialogue_box/day/forward_idle.png "Стрелка вперёд")](/resource/raw/images/gui/dialogue_box/day/forward_idle.png)

3. Закат (`$ sunset_time()`)
> [![Диалоговое окно](/resource/raw/images/gui/dialogue_box/sunset/dialogue_box.png "Диалоговое окно")](/resource/raw/images/gui/dialogue_box/sunset/dialogue_box.png)
[![Стрелка назад](/resource/raw/images/gui/dialogue_box/sunset/backward_idle.png "Стрелка назад")](/resource/raw/images/gui/dialogue_box/sunset/backward_idle.png)
[![Стрелка вперёд](/resource/raw/images/gui/dialogue_box/sunset/forward_idle.png "Стрелка вперёд")](/resource/raw/images/gui/dialogue_box/sunset/forward_idle.png)

4. Ночь (`$ night_time()`)
> [![Диалоговое окно](/resource/raw/images/gui/dialogue_box/night/dialogue_box.png "Диалоговое окно")](/resource/raw/images/gui/dialogue_box/night/dialogue_box.png)
[![Стрелка назад](/resource/raw/images/gui/dialogue_box/night/backward_idle.png "Стрелка назад")](/resource/raw/images/gui/dialogue_box/night/backward_idle.png)
[![Стрелка вперёд](/resource/raw/images/gui/dialogue_box/night/forward_idle.png "Стрелка вперёд")](/resource/raw/images/gui/dialogue_box/night/forward_idle.png)


### Изменение оттенка спрайтов

При использовании стандартных спрайтов новеллы во время повествования можно изменить их оттенок. Для этого в повествовании нужно изменить постоянную переменную "времени спрайтов".

Пример:

```renpy
    # ...
    show sl normal pioneer
    $ persistent.sprite_time = "day" # Дневной оттенок (без оттенка)
    pause
    $ persistent.sprite_time = "sunset" # Утренний/вечерний оттенок
    pause
    $ persistent.sprite_time = "night" # Ночной оттенок
    pause
    # ...
```

1. Цвет дневного оттенка — <b style="color: #FFFFFF; background: #0006; border-radius: 8px; padding: 8px">■ #FFFFFF ■</b>.
> [![Спрайт пионера](/docs/img/narration-tint-day.webp "Спрайт пионера")](/docs/img/narration-tint-day.webp)

2. Цвет утреннего/вечернего оттенка — <b style="color: #EFD1FF; background: #0006; border-radius: 8px; padding: 8px">■ #EFD1FF ■</b>.
> [![Спрайт пионера](/docs/img/narration-tint-sunset.webp "Спрайт пионера")](/docs/img/narration-tint-sunset.webp)

3. Цвет ночного оттенка — <b style="color: #A0C6D1; background: #0006; border-radius: 8px; padding: 8px">■ #A0C6D1 ■</b>.
> [![Спрайт пионера](/docs/img/narration-tint-night.webp "Спрайт пионера")](/docs/img/narration-tint-night.webp)

:::warning
Смена оттенка не распространяется на некоторые спрайты оригинальной новеллы и на все пользовательские спрайты!
Их нужно будет красить самому или добавлять проверки как в оригинальной новелле!
Пример покраски спрайта с проверкой можно найти в [файлах скриптов игры](/resource/raw/sprites.rpy).
:::

:::info
Как такового "прологового" оттенка в игре нет, в эпилоге (где используется прологовый стиль интерфейса) использован ночной оттенок.
:::

### Изменение режима повествования

Как вы уже наверное знаете в базовой игре существует два режима вывода текста. Называются они ADV- и NVL-режимами.  
В **ADV-режиме** (от англ. *Adventure* — **Приключение**) на экране отображается по одной реплике в отдельном окне диалога.
Также в этом режиме реплики часто сопровождаются визуальными и звуковыми эффектами.
**Этот режим всегда включён по умолчанию.**  
В **NVL-режиме** (от англ. *Novel* — **Новелла**) на экране может находиться большое количество реплик.

Пример:

```renpy
    # ...
    $ set_mode_adv() # Ещё раз напоминаем что ADV-режим стоит по умолчанию!
    "Короткая фраза."
    $ set_mode_nvl() # Изменяем на NVL-режим...
    "Lorem ipsum dolor sit amet..." # А здесь уже можно писать длинный текст
    # ...
```

:::warning
Смена режимов повествования таким образом работает только для ванильных и наследуемых от них персонажей!

О том, как создать NVL-персонажа, вы можете прочесть в [этой статье](/docs/characters).

Список всех ванильных персонажей вы можете найти на [этой странице](/resources/original/characters).
:::

## Больше информации

Получить больше информации о повествовании вы можете на [странице официальной документации Ren'Py](https://www.renpy.org/doc/html/dialogue.html).

## Источники

1^ Оригинальный. Источник: [Глоссарий / Ваниль](/docs/glossary#ваниль).